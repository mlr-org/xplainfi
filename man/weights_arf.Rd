% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights_arf.R
\name{weights_arf}
\alias{weights_arf}
\title{ARF-based importance sampling weights}
\usage{
weights_arf(sampler, conditioning_set = NULL)
}
\arguments{
\item{sampler}{(\link{ConditionalARFSampler}) Fitted sampler whose \code{psi} and \code{arf_model} provide the density estimate.}

\item{conditioning_set}{(\code{character()} | \code{NULL}: \code{NULL}) Features to condition on.
\code{NULL} conditions on all features except the perturbed one(s) (\link{CFI}).
A subset gives the \link{RFI} weight \eqn{p(x_j' \mid x_S) / p(x_j')}, \code{character(0)} gives constant weights (\link{PFI}).
The perturbed feature(s) are always removed from the set.}
}
\value{
(\verb{function(data, feature)}) Weight function returning a non-negative \code{numeric(nrow(data))}.
\code{data} is a \link[data.table:data.table]{data.table} of perturbed feature values (all task features),
\code{feature} the perturbed feature(s).
}
\description{
Creates a weight function for \link{PerturbationImportance} methods that turns marginal perturbation into an
importance sampling estimator of conditional importance.
For a perturbed observation \eqn{(x_j', x_{-j})}, the weight is the density ratio
\deqn{w = \frac{p(x_j' \mid x_{-j})}{p(x_j')} = \frac{p(x_j', x_{-j})}{p(x_j')\,p(x_{-j})},}
with all densities estimated by the ARF stored in \code{sampler} via \link[arf:lik]{arf::lik}.
Weighting marginally perturbed losses by \eqn{w} yields the expectation under the conditional distribution
\eqn{p(x_j \mid x_{-j})}, i.e. the \link{CFI} target, without conditional sampling.
}
\details{
Weights are unnormalized and self-normalized downstream, so only ratios matter.
For a group of features, the weights assume that each column was permuted independently, as
\link{MarginalPermutationSampler} does, so the proposal density is the product of the per-feature marginals.
The marginal densities are evaluated on the unique rows of the respective column subsets, which for permutation
sampling are the original rows, so the cost per call is one full-column \link[arf:lik]{arf::lik} call on all perturbed rows
plus two partial-column calls on the original rows.
This is comparable to conditional sampling with \link[arf:forge]{arf::forge} at typical \code{n_repeats}, since \code{forge} amortizes
its leaf lookup over \code{samples_per_row} while \code{lik} is linear in the number of perturbed rows.

The effective sample size \eqn{(\sum_i w_i)^2 / \sum_i w_i^2}, reported in \verb{$scores()} as \code{ess}, shrinks as the
conditional \eqn{p(x_j \mid x_{-j})} becomes narrower relative to the marginal \eqn{p(x_j)}, i.e. with stronger
dependence and larger conditioning sets.
A small \code{ess} indicates that the weighted estimate is driven by few observations and conditional sampling
(\link{CFI} with \code{estimator = "sampling"}) should be preferred.

Fit the sampler with \code{finite_bounds = "local"} so that leaf densities do not extend past the observed range of
each feature; unbounded leaves put mass where no data exist and distort the density ratios in the tails.
}
\examples{
\dontshow{if (requireNamespace("arf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(mlr3)
task = sim_dgp_correlated(n = 300)
sampler = ConditionalARFSampler$new(task, finite_bounds = "local", verbose = FALSE)
wf = weights_arf(sampler)
perturbed = task$data(cols = task$feature_names)[, x1 := sample(x1)]
w = wf(perturbed, "x1")
sum(w)^2 / sum(w^2) # effective sample size

# Same weights inside CFI:
cfi = CFI$new(
  task = task,
  learner = lrn("regr.rpart"),
  measure = msr("regr.mse"),
  sampler = sampler,
  estimator = "weighting",
  n_repeats = 5
)
cfi$compute()
cfi$scores()[, .(feature, iter_repeat, importance, ess)]
\dontshow{\}) # examplesIf}
}
